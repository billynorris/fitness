{
  "name": "fitness",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test && npx jest --runInBand",
    "build": "lerna run build",
    "watch": "lerna run watch --stream --parallel"
  }
}
